which celery
# /mnt/c/Users/abrah/OneDrive/official_projects/dating_app/env/bin/celery
celery --version
# Should show something like 5.x.x
Then start Celery worker
bash
Copy code
celery -A core worker -l info
And in another terminal:

bash
Copy code
celery -A core beat -l info
Now it will run from your virtual environment, no system errors.


#!/bin/bash
source env/bin/activate
redis-server --daemonize yes
python manage.py runserver 0.0.0.0:8000 &
celery -A core worker -l info &
celery -A core beat -l info &



# debug development server
gunicorn core.asgi:application -k uvicorn.workers.UvicornWorker --workers 1 --bind 0.0.0.0:8000
